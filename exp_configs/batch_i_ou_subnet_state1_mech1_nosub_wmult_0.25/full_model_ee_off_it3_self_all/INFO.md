Compare several full models with some connections turned of (i.e. replaced by static surrogate inputs).

Models:
- IT3-IT3 connections off
- Connections from exc pops to themselves off
- All connections between exc pops off